# Accuracy Logs

This folder stores Slurm logs and per-task `lm_eval` logs for quality benchmark runs.

Logs are useful for debugging failed jobs, vLLM crashes, dataset cache issues, or model loading errors. They are ignored by Git; the curated CSV outputs in `../final/` are the tracked results.
